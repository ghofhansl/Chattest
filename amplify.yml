version: 1
backend:
  phases:
    build:
      commands:
        - echo "No backend commands to run."
frontend:
  phases:
    preBuild:
      commands:
        - pip install -r requirements.txt
    build:
      commands:
        - echo "No build commands to run for Flask."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
